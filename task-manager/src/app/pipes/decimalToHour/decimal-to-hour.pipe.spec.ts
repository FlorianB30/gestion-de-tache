import { DecimalToHourPipe } from './decimal-to-hour.pipe';

describe('DecimalToHourPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalToHourPipe();
    expect(pipe).toBeTruthy();
  });
});
